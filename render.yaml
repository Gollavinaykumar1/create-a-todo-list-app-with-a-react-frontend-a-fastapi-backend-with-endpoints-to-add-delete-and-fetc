services:
  - type: web
    name: create-a-todo-list-app-with-a-react-frontend-a-fas
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: APP_ENV
        value: production
